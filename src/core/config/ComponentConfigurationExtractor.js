import { Component } from '../component';

/**
 * This class extracts component configuration from a configuration object, class object, or class constructor
 */
export default class ComponentConfigurationExtractor {

	/**
	 * @static
	 * Extracts configuration from a given object
	 * 
	 * @param  {Object/Function} obj
	 * @return {Component}
	 */
	static extractConfiguration(obj) {

	}

	static extractConfigurationFromConstructor(constructorFn) {

	}

	static extractConfigurationFromObject(cmpObject) {

	}

	static extractConfiguration(cfg) {

	}
}